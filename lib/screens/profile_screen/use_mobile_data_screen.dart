import 'package:flutter/material.dart';

class UseMobileDataScreen extends StatelessWidget {
  const UseMobileDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

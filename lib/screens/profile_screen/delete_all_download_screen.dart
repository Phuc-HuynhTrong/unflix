import 'package:flutter/material.dart';

class DeleteAllDownloadScreen extends StatelessWidget {
  const DeleteAllDownloadScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
